**Core Java (Java 17/21 LTS)**
- Java 8 refresher: Streams, Optionals, Lambdas
- Records, sealed classes, pattern matching (switch expressions)
- Lambda expressions & functional interfaces
- Stream API (Collectors, parallel streams)
- Virtual threads (Project Loom) & structured concurrency
- Text blocks, var, enhanced NIO

**Web Fundamentals**
- Servlets & JSP (legacy context only — brief overview)
- REST architecture principles
- HTTP methods, status codes, headers

**Jakarta Persistence & Spring Data**
- Jakarta Persistence API (JPA) — entities, mappings
- Relationships: One-to-One, One-to-Many, Many-to-One, Many-to-Many
- Spring Data JPA — repositories (CrudRepository, JpaRepository)
- Query methods, @Query, native vs JPQL
- Pagination & sorting
- Auditing, projections, specifications

**Spring Framework 7**
- Dependency Injection & IoC
- Component scanning, annotations (@Component, @Service, @Repository, @Controller, @Bean, @Configuration)
- Bean lifecycle & scopes
- AOP — aspects, join points, pointcuts, advice types
- Spring MVC / WebFlux (reactive overview)

**Spring Boot 4**
- Auto-configuration & starters
- Spring Initializr project setup
- Externalized configuration (profiles, YAML, env vars)
- Actuator for observability

**REST API Development**
- CRUD via GET/POST/PUT/PATCH/DELETE
- Path, query, and request body design
- API versioning & naming conventions
- OpenAPI/Swagger documentation
- File upload/download
- Testing with Postman/HTTPie

**NoSQL & Polyglot Persistence**
- Spring Data MongoDB (MongoTemplate, MongoRepository)
- Overview: Redis, Elasticsearch integration

**Spring Security 7**
- Authentication vs Authorization
- Form login, DB & in-memory auth
- Password encoding (BCrypt/Argon2)
- JWT authentication
- OAuth2 / OpenID Connect
- Method security (@PreAuthorize, @PostAuthorize)

**Transactions**
- @Transactional, propagation, isolation levels

**Microservices & Cloud-Native**
- Microservices vs monolith
- Spring Cloud (Config, Gateway, service discovery)
- Resilience patterns (circuit breaker — Resilience4j)
- API Gateway concepts

**Containerization & DevOps**
- Docker — images, containers, Dockerfile, Compose
- CI/CD basics — Jenkins/GitHub Actions
- Load/performance testing — JMeter
- Kubernetes overview (optional advanced module)
